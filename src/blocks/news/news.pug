mixin news(options)
  - let {img="img", header="header", data="data", text="text"} = options;
  .news
    img(class="news__img" src= img alt="bike")
    .news__text
      .news__text_header=  header
      .news__text_data= data
      .news__text_info= text