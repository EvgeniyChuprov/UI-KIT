mixin select(values)
  -let i = 0

  .select
    select.select__block(name="select" )
      option.select__block_none(disabled selected) Choose an option

      each val in values
        option.select__block_option(value= i += 1)= val