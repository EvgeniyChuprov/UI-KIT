mixin toggle(options)
  -  let {option= false } = options;

  label.toggle
    input.toggle__input( id="switch" type="checkbox"  checked= option)

    .toggle__on-off                          
    .toggle__ball