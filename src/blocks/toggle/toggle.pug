mixin toggle(options)
  -  let {check= false } = options;

  label.toggle
    input.toggle__input( id="switch" type="checkbox"  checked= check)

    .toggle__on-off                          
    .toggle__ball