mixin map(options)
  -let {marker="marker", lat="lat", lng="lng", zoom="zoom", city="city", title="title"} = options;
  div(id="map" class="map" data-marker= marker data-lat= lat data-lng= lng data-zoom= zoom)
  .map_menu 
    .head= title
    .adress= city
    .icon
      .locat
      .pin